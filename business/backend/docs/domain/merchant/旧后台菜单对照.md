# 旧总后台商户管理菜单映射

- 旧页在同一行承载编辑、支付、短信、流媒体、订阅、重置密码和删除。
- 新页路由仍为 `/merchants`，菜单仍为“商城中台 → 商户管理”。
- 旧列 `appid` 和误标的 `commercial_id` 不迁入。删除改为关闭终态。
- 套餐目录、支付目录、短信主数据和流媒体目录不搬进本页。
- 原误挂在 `/merchants` 的平台操作员页改到 `/users`，Contribution id 为 `identity.admin.users`。
