# 登录验证码事务

| 用例 | 同一事务 | 事务外 |
|---|---|---|
| 请求验证码 | 锁定活动店铺、过期同目的地旧 PENDING、插入散列与 `expires_at` | SYNC `Dispatch`；失败不删除行 |
| 核验验证码 | 按 challengeId 锁定、校验店铺与 TTL、比较散列、推进 CONSUMED/EXPIRED | 无 Platform 调用 |

隔离级别 `REPEATABLE READ`。非幂等外部调用不进事务。
