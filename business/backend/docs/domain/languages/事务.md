# 店铺语言事务

读取：锁外查询当前会话店铺、已发布语言行，再编排 Platform locales/entities/texts。Platform 不可达时仍返回 Identity 语言事实，完成率为 0。

写入：`identity_shop_command` + `READ COMMITTED`。锁店铺行，校验 `expectedVersion`，更新 `default_locale` 并推进店铺版本，替换该店 `identity_shop_locale` 已发布行。默认语言必须落在已发布列表中。相同 `commandKey` 且摘要相同重放。
