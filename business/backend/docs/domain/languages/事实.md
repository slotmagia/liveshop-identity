# 店铺语言事实源

- Identity 是店铺已发布语言与默认语言的事实源。`identity_shop.default_locale` 是默认语言；`identity_shop_locale` 是已发布语言集合。
- 平台目标语言目录和译文完成率来自 Platform Internal localization，不写入 Identity 表。
- `merchant_id + shop_id` 来自 merch 会话。浏览器不得提交归属范围，不得直打 Platform Internal。
- 源语言 `zh-CN` 始终可发布。第一期平台目标语言是 `en-US`。
