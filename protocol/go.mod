// The protocol module is published on its own so consumers depend on
// identity's wire contract without depending on its implementation.
module github.com/lvtuopen-ai/liveshop-identity/protocol

go 1.25.0
